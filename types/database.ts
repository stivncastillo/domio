/**
 * Placeholder de los tipos generados por Supabase.
 *
 * Una vez tengas el proyecto de Supabase creado y las tablas de
 * supabase/migrations/0001_init.sql aplicadas, reemplaza este archivo
 * corriendo (con la Supabase CLI):
 *
 *   npx supabase gen types typescript --project-id TU-PROJECT-ID > types/database.ts
 *
 * Eso te da autocompletado y chequeo de tipos exacto contra tu schema
 * real. Por ahora dejamos un tipo minimo para que el cliente compile.
 */
export type Database = Record<string, unknown>;
